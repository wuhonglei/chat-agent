# Chat Agent 产品功能 Roadmap

## 1. 背景与目标

当前 Chat Agent 已具备多轮对话、MCP 工具调用与文件操作能力，但随着能力增强，系统面临三类核心挑战：

- 安全边界不足（命令执行与文件访问风险高）
- 可观测能力不足（LLM 成本、延迟、失败根因难定位）
- 扩展能力缺乏规范（Skills 生态、商业化能力未体系化）

本 Roadmap 的核心目标是按阶段建立：**安全可控 -> 可观测可诊断 -> 能力可扩展 -> 商业化可交付**。

## 2. 优先级原则

优先级排序遵循以下原则：

1. 先守住系统安全底线，再扩展能力上限
2. 先建设核心链路观测，再建设通用系统监控
3. 技能扩展必须建立在沙箱与规范化协议之上
4. 商业化能力在基础能力稳定后推进

对应优先级：

- **P0**：必须立即处理，存在安全红线
- **P1**：高优先级，影响架构基础和核心调试能力
- **P2**：中优先级，提升健壮性和扩展效率
- **P3**：低优先级，偏商业化和部署增强

## 3. 分阶段规划


| 阶段      | 时间窗口  | 重点项                       | 阶段目标           |
| ------- | ----- | ------------------------- | -------------- |
| Phase 0 | 本周    | #4 命令执行沙箱隔离 + #5 用户目录虚拟映射 | 守住安全底线，解除路径耦合  |
| Phase 1 | 2 周内  | #2 LLM 观测体系               | 建立模型与工具链路可诊断能力 |
| Phase 2 | 1 个月内 | #1 Prometheus 监控指标        | 完善系统级与业务级可观测性  |
| Phase 3 | 按需迭代  | #3 Agent Skills 技能扩展      | 在安全与观测基础上扩展能力  |
| Phase 4 | 商业化阶段 | #6 自定义域名支持                | 支持多租户/独立部署场景   |


## 4. 特性规划明细

### 4.1 #4 命令执行沙箱隔离（P0）

**目标**
所有命令执行与高风险工具调用必须在受限环境中运行，避免对宿主环境造成破坏。

**范围（MVP）**

- 引入统一执行抽象（如 `SandboxExecutor`）
- 限制可访问目录（仅会话工作区可读写）
- 限制网络、系统调用与资源额度（CPU/内存/时长/进程数）
- 命令策略控制（白名单或策略引擎）
- 审计日志（记录拒绝原因、调用上下文、会话信息）

**建议方案**

- 优先容器化或轻量隔离方案（firejail/gVisor/namespace）
- 预留多后端适配能力，避免与单一沙箱技术强耦合

**交付物**

- 沙箱执行模块与配置
- 风险命令拦截策略
- 沙箱审计日志与告警规则
- 安全测试用例集

**验收标准**

- 危险命令、越权访问均可拦截
- 非授权路径访问拦截率 100%
- 合法操作主流程可用性 >= 99%

---

### 4.2 #5 用户目录虚拟映射（P1）

**目标**
对外暴露稳定虚拟路径，屏蔽内部物理路径结构，降低安全风险并解耦存储后端。

**范围（MVP）**

- 新增虚拟路径层（例如 `/workspace/current/`）
- 统一路径解析入口，禁止直接使用物理路径
- 路径规范化与安全校验（防路径遍历/符号链接逃逸）
- 新旧接口兼容与迁移

**交付物**

- 虚拟路径规范文档
- 路径映射组件与安全校验模块
- 回归测试（上传、读取、移动、删除等文件场景）

**验收标准**

- 前后端不再出现真实物理路径
- 路径遍历攻击测试 100% 拦截
- 文件工具调用成功率不低于改造前基线

---

### 4.3 #2 LLM 观测体系（P1）

**目标**
建立 Chat Agent 全链路可观测能力，能够快速定位模型问题、上下文问题与工具链路问题。

**范围（MVP）**

- 打通端到端 trace：请求 -> 编排 -> 模型 -> 工具 -> 响应
- 采集核心指标：输入/输出 token、延迟、错误率、重试次数、工具耗时
- 统一关联维度：`trace_id`、`conversation_id`、`user_id`
- 错误分类：模型异常、工具异常、网络异常、超时异常

**建议方案**

- 优先接入 Langfuse 或同类平台
- 保留 OpenTelemetry 扩展点，支持后续多观测后端

**交付物**

- 埋点规范文档
- 观测面板（按模型、技能、接口维度查看）
- 故障定位手册

**验收标准**

- 95% 以上请求具备可追踪链路
- 能在 10 分钟内定位典型失败请求根因
- 可按用户与会话维度查询 token 与耗时分布

---

### 4.4 #1 Prometheus 监控指标（P2）

**目标**
构建系统健康与业务健康双层监控与告警体系。

**范围（MVP）**

- 系统指标：QPS、P95/P99、错误率、超时率、资源占用
- 业务指标：会话轮数、工具调用成功率、token 消耗速率、中断率
- 告警规则：错误率飙升、token 异常、工具失败率异常

**交付物**

- `/metrics` 暴露与抓取配置
- Grafana 仪表板
- 值班告警策略与阈值说明

**验收标准**

- 核心接口全部可观测
- 至少 3 条核心告警规则上线并验证有效

---

### 4.5 #3 Agent Skills 技能扩展（P2）

**目标**
在统一安全与协议框架下扩展技能生态，提升用户任务完成率。

**范围（MVP）**

- 技能元数据规范（输入输出 schema、权限、风险等级）
- 标准加载协议（注册、鉴权、版本管理、灰度开关）
- 首批高频技能（文件处理、搜索、数据分析、代码执行）
- 所有技能执行统一接入沙箱与观测链路

**交付物**

- Skills 接入规范文档
- 官方技能模板与示例
- 技能质量基线（成功率、平均耗时、失败分类）

**验收标准**

- 新技能接入耗时 <= 1 天
- 技能执行可追踪、可熔断、可回滚

---

### 4.6 #6 自定义域名支持（P3）

**目标**
支持多租户或商业化部署时的独立域名访问能力。

**范围（MVP）**

- 域名绑定与证书自动化
- 网关/Nginx 动态配置与热更新
- 子域名或子路径路由策略

**交付物**

- 域名管理接口
- 部署与运维手册
- 域名接入自动化脚本（可选）

**验收标准**

- 新域名接入时间 <= 10 分钟
- 默认域名与现有业务不受影响

## 5. 依赖关系与执行顺序

关键依赖如下：

- #4 沙箱隔离是 #3 Skills 扩展的前置条件
- #5 虚拟映射建议与 #4 并行推进，统一安全路径策略
- #2 LLM 观测优先于 #1 Prometheus，先解决 agent 核心链路可诊断

推荐执行顺序：

1. #4 + #5
2. #2
3. #1
4. #3
5. #6

## 6. 里程碑与检查点

### M1（本周末）

- 沙箱执行方案评审通过
- 虚拟路径方案评审通过
- 安全测试清单冻结

### M2（2 周内）

- LLM 全链路埋点上线
- 可完成单请求根因定位演练

### M3（1 个月内）

- Prometheus + Grafana + 告警体系上线
- 形成周度稳定性看板

### M4（持续）

- 技能按优先级逐步扩展并灰度发布
- 每月复盘技能效果和失败模式

## 7. 风险与应对

- **风险：沙箱引入后性能下降**
应对：先灰度，压测对比，设置资源上限与快速回退开关。
- **风险：路径映射改造影响现有文件逻辑**
应对：保留兼容层，逐步迁移，关键链路回归测试先行。
- **风险：观测埋点过多导致噪声与成本上升**
应对：定义最小必要埋点与采样策略，分级存储与保留期管理。
- **风险：技能扩展导致质量不稳定**
应对：建立技能准入标准、灰度机制与熔断策略。

## 8. 成功标准（季度视角）

当以下条件满足时，可认为本轮 Roadmap 达成：

- 高风险命令与越权访问风险可控（安全红线建立）
- 核心请求链路可追踪、可定位、可复盘（可观测建立）
- 技能扩展从“项目制接入”升级为“标准化接入”（可扩展建立）
- 支持后续商业化部署需求（可交付建立）
