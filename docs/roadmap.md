# Chat Agent 产品功能 Roadmap

## 1. 背景与目标

当前 Chat Agent 已具备多轮对话、MCP 工具调用与文件操作能力，但随着能力增强，系统面临三类核心挑战：

- 安全边界不足（命令执行与文件访问风险高）
- 可观测能力不足（LLM 成本、延迟、失败根因难定位）
- 扩展能力缺乏规范（Skills 生态、商业化能力未体系化）

本 Roadmap 的核心目标是按阶段建立：**安全可控 -> 可观测可量化 -> Agent 能力可证明 -> 商业化可交付**。

## 2. 优先级原则

优先级排序遵循以下原则：

1. 先守住系统安全底线，再扩展能力上限
2. 先建设核心链路观测，再建设通用系统监控
3. 技能扩展必须建立在沙箱与规范化协议之上
4. 商业化能力在基础能力稳定后推进

对应优先级：

- **P0**：必须立即处理，存在安全红线
- **P1**：高优先级，影响架构基础和核心调试能力
- **P2**：中优先级，提升健壮性和扩展效率
- **P3**：低优先级，偏商业化和部署增强

## 3. 分阶段规划

采用双轨推进：**平台能力轨**（安全、观测、规范化）与**指标建设轨**（Eval、记忆、RAG）。平台能力保障可落地，指标建设保障可证明。


| 阶段      | 时间窗口  | 平台能力轨重点项                                   | 指标建设轨重点项                            | 阶段目标                 |
| ------- | ----- | ------------------------------------------ | ----------------------------------- | -------------------- |
| Phase 0 | 本周    | #4 命令执行沙箱隔离 | 指标口径冻结（基线版本、统计窗口、分位数、样本量门槛）         | 守住安全底线，建立统一统计口径 |
| Phase 1 | 2 周内  | #2 LLM 观测体系                                | Eval：100+ case 集建设 + 自动评分一致性首轮验证    | 建立核心链路可诊断与可评测能力      |
| Phase 2 | 1 个月内 | #1 Prometheus 监控指标                         | 记忆：跨会话命中率与重复问题下降跟踪；RAG：相关性与检索延迟基线对比 | 建立周度量化复盘与效果-时延平衡机制   |
| Phase 3 | 按需迭代  | #3 Agent Skills 技能扩展                       | Eval/记忆/RAG 指标持续优化与回归防退化            | 在安全与观测基础上扩展能力并稳定提质   |
| Phase 4 | 商业化阶段 | #6 自定义域名支持                                 | 对外输出阶段性指标报告（能力证明材料）                 | 支持多租户/独立部署并具备对外可证明成果 |


## 4. 特性规划明细

### 4.1 #4 命令执行沙箱隔离（P0）

**目标**
所有命令执行与高风险工具调用必须在受限环境中运行，避免对宿主环境造成破坏。

**范围（MVP）**

- 引入统一执行抽象（如 `SandboxExecutor`）
- 限制可访问目录（仅会话工作区可读写）
- 限制网络、系统调用与资源额度（CPU/内存/时长/进程数）
- 命令策略控制（白名单或策略引擎）
- 审计日志（记录拒绝原因、调用上下文、会话信息）

**建议方案**

- 优先容器化或轻量隔离方案（firejail/gVisor/namespace）
- 预留多后端适配能力，避免与单一沙箱技术强耦合

**交付物**

- 沙箱执行模块与配置
- 风险命令拦截策略
- 沙箱审计日志与告警规则
- 安全测试用例集

**验收标准**

- 危险命令、越权访问均可拦截
- 非授权路径访问拦截率 100%
- 合法操作主流程可用性 >= 99%

---

### 4.2 #2 LLM 观测体系（P1）

**目标**
建立 Chat Agent 全链路可观测能力，能够快速定位模型问题、上下文问题与工具链路问题。

**范围（MVP）**

- 打通端到端 trace：请求 -> 编排 -> 模型 -> 工具 -> 响应
- 采集核心指标：输入/输出 token、延迟、错误率、重试次数、工具耗时
- 统一关联维度：`trace_id`、`conversation_id`、`user_id`
- 错误分类：模型异常、工具异常、网络异常、超时异常

**建议方案**

- 优先接入 Langfuse 或同类平台
- 保留 OpenTelemetry 扩展点，支持后续多观测后端

**交付物**

- 埋点规范文档
- 观测面板（按模型、技能、接口维度查看）
- 故障定位手册

**验收标准**

- 95% 以上请求具备可追踪链路
- 能在 10 分钟内定位典型失败请求根因
- 可按用户与会话维度查询 token 与耗时分布

---

### 4.3 #1 Prometheus 监控指标（P2）

**目标**
构建系统健康与业务健康双层监控与告警体系。

**范围（MVP）**

- 系统指标：QPS、P95/P99、错误率、超时率、资源占用
- 业务指标：会话轮数、工具调用成功率、token 消耗速率、中断率
- 告警规则：错误率飙升、token 异常、工具失败率异常

**交付物**

- `/metrics` 暴露与抓取配置
- Grafana 仪表板
- 值班告警策略与阈值说明

**验收标准**

- 核心接口全部可观测
- 至少 3 条核心告警规则上线并验证有效

---

### 4.4 #3 Agent Skills 技能扩展（P2）

**目标**
在统一安全与协议框架下扩展技能生态，提升用户任务完成率。

**范围（MVP）**

- 技能元数据规范（输入输出 schema、权限、风险等级）
- 标准加载协议（注册、鉴权、版本管理、灰度开关）
- 首批高频技能（文件处理、搜索、数据分析、代码执行）
- 所有技能执行统一接入沙箱与观测链路

**交付物**

- Skills 接入规范文档
- 官方技能模板与示例
- 技能质量基线（成功率、平均耗时、失败分类）

**验收标准**

- 新技能接入耗时 <= 1 天
- 技能执行可追踪、可熔断、可回滚

---

### 4.5 #6 自定义域名支持（P3）

**目标**
支持多租户或商业化部署时的独立域名访问能力。

**范围（MVP）**

- 域名绑定与证书自动化
- 网关/Nginx 动态配置与热更新
- 子域名或子路径路由策略

**交付物**

- 域名管理接口
- 部署与运维手册
- 域名接入自动化脚本（可选）

**验收标准**

- 新域名接入时间 <= 10 分钟
- 默认域名与现有业务不受影响

## 5. 依赖关系与执行顺序

### 5.1 平台能力轨依赖

- #4 沙箱隔离是 #3 Skills 扩展的前置条件（无安全隔离不扩工具能力）
- #2 LLM 观测优先于 #1 Prometheus，先建立 Agent 核心链路可诊断、可量化能力

### 5.2 指标建设轨依赖

- Eval 指标建设依赖 #2（trace 与事件埋点），无统一链路数据无法稳定自动评分
- 记忆指标建设依赖 Eval 基线与观测口径，先定义“可命中请求”再统计命中率
- RAG 指标建设依赖检索链路埋点（召回/重排/注入），并以统一基线做 A/B 对比
- 三类指标共用第 8 章口径：滚动 7 天窗口、P50/P95、样本量门槛

### 5.3 合并执行顺序（双轨）

1. **Phase 0（平台先行）**：#4，并冻结第 8 章指标口径
2. **Phase 1（可诊断 + 可评测）**：#2 + Eval（100+ case + 一致性校准）
3. **Phase 2（系统监控 + 指标扩展）**：#1 + 记忆指标 + RAG 指标
4. **Phase 3（能力扩展 + 防退化）**：#3 + Eval/记忆/RAG 持续优化与回归闭环
5. **Phase 4（商业化）**：#6 + 对外能力证明材料沉淀（指标报告/案例）

## 6. 里程碑与检查点

### M1（本周末）

- 沙箱执行方案评审通过
- 安全测试清单冻结
- 指标口径文档冻结（Eval / 记忆 / RAG 的基线与统计口径）

### M2（2 周内）

- LLM 全链路埋点上线
- 可完成单请求根因定位演练
- Eval 评测集达到 100+ case，完成首轮自动评分一致性校准（>=85%）

### M3（1 个月内）

- Prometheus + Grafana + 告警体系上线
- 形成周度稳定性看板
- 记忆指标进入稳定观测：跨会话命中率、重复问题下降、误命中率周报化
- RAG 指标进入稳定观测：相关性提升与检索延迟（P50/P95）周报化

### M4（持续）

- 技能按优先级逐步扩展并灰度发布
- 每月复盘技能效果和失败模式
- Eval/记忆/RAG 指标持续优化，建立退化自动告警与回归修复闭环

## 7. 风险与应对

- **风险：沙箱引入后性能下降**
应对：先灰度，压测对比，设置资源上限与快速回退开关。
- **风险：观测埋点过多导致噪声与成本上升**
应对：定义最小必要埋点与采样策略，分级存储与保留期管理。
- **风险：技能扩展导致质量不稳定**
应对：建立技能准入标准、灰度机制与熔断策略。

## 8. 指标与验收标准

为确保阶段成果可量化、可复盘、可对外说明，本节定义统一指标口径与分项验收标准。指标按优先级聚焦：**Eval -> 记忆 -> RAG**。

### 8.1 统一统计口径（适用于全部指标）

- **基线版本固定**：每个指标必须声明对照组（如 v0：无记忆 / 无重排 / 旧评测方案）
- **统计窗口统一**：默认按滚动 7 天统计，按周复盘
- **分位数统一**：时延类指标统一使用 P50 / P95，不仅看平均值
- **样本量门槛**：样本不足时仅记录趋势，不宣称提升结论
- **可追踪维度**：按 `trace_id`、`conversation_id`、`user_id`、`scenario_type` 回溯

### 8.2 Eval 指标与验收（P0）

**目标**
建立可持续迭代的 Agent 评测基线，确保质量变化可被自动发现。

**核心指标**

- **100+ case 覆盖率**：已纳入自动评测的数据集 case 数 / 目标 case 数（>=100）
- **自动评分一致性**：自动评分与人工复核结果一致比例

**MVP 范围**

- 评测集至少覆盖 4 类场景：工具调用、RAG 问答、多轮对话、边界异常
- 每周抽样 30~50 条样本进行人工复核
- 输出按场景分桶的通过率与失败分类

**验收标准**

- case 总量 >= 100，且 4 类场景均有有效样本
- 自动评分一致性 >= 85%（阶段目标），持续优化至 90%
- 能按场景定位退化项，并在一周内完成回归修复闭环

### 8.3 记忆指标与验收（P1）

**目标**
验证跨会话记忆对用户体验的真实提升，并控制误用风险。

**核心指标**

- **跨会话命中率**：成功命中并使用历史记忆的请求数 / 可使用记忆的请求数
- **重复问题下降**：引入记忆后用户重复提问率相对基线下降比例
- **记忆误命中率（守门指标）**：错误引用历史记忆导致答非所问的比例

**MVP 范围**

- 建立“可使用记忆请求”的判定规则（避免命中率被高估）
- 记录命中后是否被实际消费（注入但未使用不计入有效命中）
- 对误命中样本进行分类（抽取错误 / 检索错误 / 注入策略错误）

**验收标准**

- 跨会话命中率 >= 60%
- 重复问题下降 >= 25%
- 记忆误命中率 < 5%
- 能输出命中收益与误命中原因的周报

### 8.4 RAG 指标与验收（P1）

**目标**
在可控时延下提升回答相关性，形成可复现的检索优化收益。

**核心指标**

- **相关性提升**：相对基线方案（如纯向量检索）在答案相关性评分上的提升比例
- **检索延迟**：检索链路端到端耗时（Retriever + Reranker），按 P50/P95 统计

**MVP 范围**

- 固定对照组（无重排或旧检索链路）并进行 A/B 对比
- 记录 top-k 召回、重排后 top-n 注入的关键中间结果
- 输出“效果-时延”双维度看板，避免只追求相关性导致时延失控

**验收标准**

- 相关性提升 >= 20%（阶段目标）
- 检索延迟 P95 <= 400ms（阶段目标）
- 当相关性提升与延迟冲突时，按预设策略优先保证 P95 时延红线

## 9. 成功标准（季度视角）

当以下条件满足时，可认为本轮 Roadmap 达成：

- 高风险命令与越权访问风险可控（安全红线建立）
- 核心请求链路可追踪、可定位、可复盘（可观测建立）
- Eval 指标达标：100+ case 稳定覆盖，自动评分一致性达到阶段目标
- 记忆指标达标：跨会话命中率与重复问题下降达到阶段目标，误命中率可控
- RAG 指标达标：相关性相对基线提升且检索时延满足 P95 红线
- 技能扩展从“项目制接入”升级为“标准化接入”（可扩展建立）
- 支持后续商业化部署需求（可交付建立）
