## 生成非对称密钥

使用 OpenSSL 命令行生成非对称密钥 RSA

```bash
# 生成 2048 位的 RSA 私钥
openssl genrsa -out private_key.pem 2048

# 或者生成 4096 位更安全的私钥
openssl genrsa -out private_key.pem 4096
```

从私钥生成公钥
```bash
openssl rsa -in private_key.pem -pubout -out public_key.pem
```