## 没有语言设置 (使用 hljs.highlightAuto('<?php echo "hello"; ?>')) 自动检测

```
// 登录模块
async function doLogin(page) {}

// 登录态保存和读取模块
async function setCookies(page) {}

// 快照截取模块
async function captureSnapshot(page, scene) {}

// 快照数据转化为节点模块
async function getAllNodes(filename) {}

// 获取组件和控制器节点模块
function getCompsAndCtls(nodes) {}

// 获取事件监听模块
async function getEventListeners(filename) {}

// 获取定时器模块
async function getTimers(filename) {}

// 计算节点内存大小模块
async function getNodesHeapSize(nodes) {}

// 获取最短引用路径模块
function bfs(sourceNode, endName = `Window / ${domain}`) {}
```

## 行内代码

`inline-block`
行内代码 `行内代码`
